import 'package:flutter/material.dart';

class FloorMapPage extends StatelessWidget {
  const FloorMapPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
