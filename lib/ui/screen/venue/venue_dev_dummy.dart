import 'package:flutter/cupertino.dart';

class VenueDevDummy extends StatelessWidget {
  const VenueDevDummy({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Only production...',
      ),
    );
  }
}
