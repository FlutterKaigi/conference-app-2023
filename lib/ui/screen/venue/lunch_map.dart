import 'package:flutter/material.dart';

class LaunchMapPage extends StatelessWidget {
  const LaunchMapPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
